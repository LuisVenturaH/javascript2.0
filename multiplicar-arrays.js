let coleccion = [5, 7, 10, 13, 2, 1, 1, 3];
// Multiplicar todos los elementos del array

let resultado = 1; // Lo igualo a uno porque lo voy a multiplicar


for (let i=0; i < coleccion.length; i++) {
    resultado *= coleccion[i]; // Es lo mismo que resultado = resultado * coleccion[i];
}

console.log(resultado)


// for (let i=1; i<coleccion.length; i++) {
//     resultado *= coleccion[i]
//     }

//     console.log(resultado);

    // Sumar todos los elementos del array hasta encontrar el número 1

    

    // Hacerlo con un bucle While:

    // let resultadoWhile = 0;
    // let i =0;

    // while (coleccion[i] != 1) {
    //     resultadoWhile += coleccion[i++];
    // }
    // console.log(resultadoWhile);


